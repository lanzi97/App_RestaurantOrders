<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" >

    <!-- View padding with inner things -->
    <padding
        android:bottom="@dimen/shape_rectangle_padding"
        android:left="@dimen/shape_rectangle_padding"
        android:top="@dimen/shape_rectangle_padding"
        android:right="@dimen/shape_rectangle_padding"></padding>


    <!-- View background color -->
    <solid
        android:color="#ffffff" >

    </solid>

    <!-- View border color and width -->
    <stroke
        android:width="2dp"
        android:color="#00000000" >
    </stroke>

    <!-- The radius makes the corners rounded -->
    <corners
        android:radius="4dp"   >
    </corners>

</shape>